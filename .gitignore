__pycache__/
*.pyc
.venv/
venv/
.env
*.sqlite
*.db
run.sh
storage/
downloaded_files/
.DS_Store
# 외부 레퍼런스(별도 repo) — 코어와 분리 보관, 커밋 제외
references/

# 비밀키(절대 커밋 금지)
deploy.sh
# ★ 디렉터리를 통째로 빼면 하위 파일 예외가 안 먹는다(git 동작) — data/* 로 적어야 한다
data/*
# 사고 원장·월간 지표는 배포 이미지에 실려야 한다(서버엔 git이 없다).
# ★ 패턴 줄에 인라인 주석을 달면 그 주석까지 패턴이 된다(git 동작) — 주석은 따로 쓴다.
!data/incidents.jsonl
!data/immune_metrics.json
*.zip

# pytest
.pytest_cache/
.prod-snapshot.sqlite*

# 브라우저 자동화 잔여물 — 페이지 스냅샷에 API 키가 찍혀 GitHub push가 막혔다(2026-08-13)
.playwright-mcp/
captcha-dialog.png
