__pycache__/
*.pyc
.venv/
venv/
.env
*.sqlite
*.db
run.sh
storage/
downloaded_files/
.DS_Store
# 외부 레퍼런스(별도 repo) — 코어와 분리 보관, 커밋 제외
references/

# 비밀키(절대 커밋 금지)
deploy.sh
# ★ 디렉터리를 통째로 빼면 하위 파일 예외가 안 먹는다(git 동작) — data/* 로 적어야 한다
data/*
!data/incidents.jsonl      # 사고 원장은 배포에 실려야 한다(코드 트리)
*.zip

# pytest
.pytest_cache/
