"""🔬 네이버 AI 역설계 — AI가 뽑는 글의 인자를 실측으로 안다.

AI를 속이는 게 아니라 'AI가 뽑을 수밖에 없는 형태'를 아는 것이 목표다.
이미 상위노출·브리핑 인용된 글이 네이버 AI의 정답지이고, 인용수가 공개 집계되므로
정답에 라벨까지 붙어 있다. 우리 계정으로는 아무 실험도 하지 않는다(R1).

이 패키지는 관측·분석 레이어다. 생성·발행 경로를 건드리지 않는다(R9).
"""
