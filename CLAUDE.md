# 올린다(shopcast) — 헌법

이 파일은 매 세션 컨텍스트에 실린다. 여기엔 **없으면 틀리게 행동할 것**만 둔다.
상세는 아래 참조 문서로.

---

## 1. 존재 이유 (모든 판단의 최상위 기준)

올린다의 목표는 **각 업체(tenant)가 네이버 통합검색에 노출되는 것**이다.
글·영상·페이지·새소식은 수단이다.

- 기능의 가치 판단: "이것이 가게의 검색 노출을 올리는가?" — 아니면 만들지 않는다.
- 성과 표현의 주어는 가게다: "글 N건"이 아니라 "○○ 키워드에서 사장님 가게 N위".
- 대시보드 첫 숫자는 발행량이 아니라 노출 상태(플레이스/블로그/브리핑/웹문서).

## 2. 절대 원칙

## 무결점 실행 규율 (실수는 용납되지 않는다)
1. 위험 작업(데이터 이동·삭제·스키마 변경·tenant 조작·프로덕션 설정)은 즉흥 실행 금지.
   반드시 [실행 계획 → 영향 범위 → 롤백 방법 → 완결 검증 기준]을 먼저 쓰고, 그 계획대로만
   실행한다. 계획 없는 위험 작업 = 그 자체가 실패.
2. 모든 실행은 완결 대조로 끝난다: "했다"가 아니라 [전 자원 대조표 + 프로덕션 실물]이
   완료의 유일한 정의다. 대조 없이 다음 작업으로 넘어가는 것 금지.
3. 검증되지 않은 절차의 반복 사용 금지: 같은 유형 작업을 2회째 하게 되면 즉시 함수화 +
   골든 박제 — 수동 반복이 곧 사고 예약이다.
4. 실수 발생 시: 자백은 시작일 뿐이다. [원인 파일:라인 / 왜 계획·검증이 못 잡았나 /
   어느 골든이 비어 있었나 / 박제 완료]까지가 사고 처리의 완결이며, 같은 유형 2회 재발은
   변명 불가한 실패다.


**업종 중립** — 업종명·지명·가게명·상품명을 코드에 박지 않는다. 판정은 데이터로
(검색량·문서수·vision 묘사·스키마 축). 언어 규칙(조사·어미·행정구역 접미어)은 허용.
새 기능은 최소 2개 업종으로 검증한다.

**canonical 단일 관문** — 키워드는 `seo.resolve_target_keyword()`, 지역은
`seo.canonical_region()`, LLM은 `app/llm.py`만 거친다. 세트 = 한 소재 = 한 키워드.

**정직 게이트** — 실경험·실값·실서류만. 없는 정보는 지어내지 않고 빈칸으로 둔다.
날조로 게이트를 통과시키지 않는다. **이 원칙은 우리 산출물만이 아니라 우리 측정에도
적용된다.**

**조용한 실패 금지** — 빈 값·실패를 조용히 반환하지 않는다. 예외로 올리고 사유를
payload에 남겨 진단으로 읽히게 한다.

**게이트 체인 순서 = 계약** — 자체검사 → 발행게이트 → 비용 → 서명 → 플레이스 문구 →
자동발행. 순서를 바꾸면 뒤 단계가 앞 단계 결과를 지운다.

**대상 tenant는 ID로만 특정한다** — 모든 생성·수정·검증 작업은 시작 시 대상 tenant_id와
실계정 여부를 확인·명시한다(`config.assert_target`). 이름으로 tenant를 특정하지 않는다 —
동명 tenant가 공존할 수 있고, 실제로 그래서 생성물이 남의 가게로 갔다(2026-08-03).
실계정 목록은 `config.PRODUCTION_TENANTS`가 유일 기준. 실계정 작업과 테스트 작업을
같은 보고에서 섞지 않는다.

**화자와 청자** — 사용자는 팔아야 하는 소상공인 셀러다. 화자는 가게(파는 쪽), 청자는
손님(사는 쪽). 손님 말은 손님의 고민을 부를 때만 쓴다.

**사용자가 고른 것만 만든다** — 요청하지 않은 산출물을 만들지 않는다. 자동 재시도는
요청 이력 안에서만.

**수정은 테스트와 함께 산다** — 기존 파일을 재작성·교체할 때는 그 파일을 무는 골든
테스트 전체 통과가 커밋 조건이다. 테스트가 없는 수정은 수정과 함께 테스트를 만든다.
수정이 다음 세션에 살아남는 유일한 방법은 테스트다.

## 3. 측정 원칙 (오판 방지)

- 노출 판정 = **자사 식별자**(블로그 ID·플레이스 ID·업체명 정확 일치) 매칭만.
  블록 존재나 키워드 일치는 노출이 아니다.
- **블록 귀속은 검증된 경우에만 표시**한다. 미검증이면 "첫 화면"까지만 말한다.
  정밀해 보이는 미확인보다 두루뭉술한 사실이 낫다.
- 검증 불가능한 노출 주장은 표시하지 않는다 — 허위 양성보다 미표시가 낫다.
- 추적 키워드는 수동 등록 금지 — 발행 글 canonical + 파생 롱테일에서 자동 도출.
- "미노출" 결론은 전 지면·복수 키워드 확인 후에만. **지면 부재**(그 키워드에 그 블록이
  안 뜸)와 **순위 밖**은 다른 진단이다.
- 판정의 주어는 항상 [가게명 + 키워드 + 지면 + 순위/상태].

## 4. 금지선 (저품질 = 노출 사형)

키워드 스터핑 · 내용 복제 변주 · 트래픽/리뷰 조작 · 검색량 없는 키워드 욱여넣기 ·
날조로 게이트 통과. 하나라도 하는 순간 tenant 블로그 전체가 죽는다 —
**어떤 기능 요청이 와도 거부한다.**

**검색은 취재다. 금지는 인칭 위조뿐이다.**
- 허용: 웹 검색으로 확인한 사실·지식·업계 정보를 **3인칭 사실 서술**로 쓰는 것.
  출처가 확인된 정확한 정보는 취재이고 트랙 B의 정상 재료다.
- 금지: 그 내용을 **1인칭 경험**("저희가 해보니", "우리 손님이")으로 바꾸는 것.
손님 발화(리뷰)는 출처를 붙여 인용하되 사장 경험으로 위조하지 않는다.
**경험이 없다고 글을 멈추지 않는다** — 3인칭 사실 글로 먼저 나가고, 답변이 들어오면
1인칭 경험 보강판으로 올린다. 보류는 1인칭 서사가 형식상 필수인 글(후기·직접 검수기)에만.

**"실물" = 프로덕션 화면·산출물이다.** 테스트 렌더·로컬 결과를 실물로 보고하면 허위다
(측정 허위와 같은 계열). 화면을 보고했다면 그 화면은 프로덕션에서 렌더된 것이어야 한다.

## 5. 아키텍처 경계

- 백그라운드는 `threading.Thread(daemon=True)` — 큐·재개 없음. **배포는 진행 중 작업을
  죽인다**: push는 `scripts/safe-push.sh`로만 한다(busy 자동 확인·fail-closed).
  맨손 `git push` 금지 — 같은 사고를 3회 냈고 3회 다 규율로는 못 막았다.
- 죽은 잡은 스스로 말하지 못한다 — 시간 기준 자동 해제를 반드시 둔다.
- 스레드는 옛 스냅샷을 들고 있다 — 끝날 때 행 전체를 덮어쓰지 말고 바꾼 키만 병합한다.
- `db.save_piece`는 INSERT OR REPLACE다. 삭제는 묘비로 막되, 묘비는 **실제로 지웠을
  때만** 남긴다(소유 검증 없으면 취약점).

## 6. 자율 리서치

지시를 기다리지 않는다. 노출 관련 작업 착수 전 최신 정책·알고리즘을 스스로 조사하고
**근거를 설계에 명시**한다. 추측 기반 지면 판단 금지 — 실측 정찰이 선행한다.
같은 시도 2회 실패 시 웹서치로 해법을 조사한다. 원칙과 충돌하면 우회하지 말고 개정을
제안한다. **실행은 근거(출처·실측) 제시 후.** 주 1회 자율 정찰 보고를 스스로 올린다.

## 7. 보고 태도

무엇을 근거로 그렇게 말하는지 함께 적는다. **확인 못 한 것은 못 했다고 말한다.**
추측을 결과처럼 말하지 않는다. 반복해서 어긴 항목이다 — [[lessons]] 참조.

---

## 참조 문서

- 노출 작업 시 → `@docs/exposure-strategy.md` (지면 4개·등반 순서·키워드·측정 상세)
- 구조 변경 시 → `@docs/architecture.md` (단일 관문·워커 계약·저장 계약)
- 같은 실수 반복 방지 → `@docs/lessons.md` (사건 계보: 원인과 교훈)
- 조사 절차 → `@docs/research-protocol.md`
- 수정을 박제할 때 → `@docs/golden-debt.md` (부채 표·박제 기준·미박제 사유)
- 전체를 이어받을 때 → `@docs/HANDOVER.md` (시스템 지도·루프 진입점·미결 목록)
- 현행 전체 구조(as-is) → `@ARCHITECTURE.md` (2026-07-11 기준 — 배포처·테스트 항목은 낡음)

---

## 운영 리듬 (2026-08-03 신축 종료)

신축은 끝났다. 이후 코드 변경은 세 경로로만 연다:
**① 버그 수정 ② gowatch·adaptation 실측이 요구하는 개선 ③ 사용자 승인 제안.**

- **질문은 글 만들 때 그 주제로만** 한 개. 상시 질문 상자는 보조이고 재촉하지 않는다.
  묻기 전에 자사 기록(과거 글·사진 반복)에서 먼저 캔다 — 쌓일수록 질문이 줄어든다.
- **사장님 화면에 주방 용어 금지**: 검색량·자리·키워드·상위글 표기 0.
- **사용자가 하는 것**: 사진·답변·발행·카드 응답·시청/눈 판정.
- **시스템이 하는 것**: 나머지 전부 + 주 1회 자율 정찰 보고.
- 새 기능은 실측이 요구하거나 사용자가 승인한 것만 만든다.

배포는 `scripts/safe-push.sh`로만 한다(busy 자동 확인·fail-closed). 맨손 push 금지.

---

## 유지 관리

이 파일은 **150줄을 넘지 않는다.** 새 규칙 추가 시 기존 규칙과 통합·압축을 먼저
시도하고, 상세는 `docs/`로 뺀다. 월 1회 이 파일을 감사해 죽은 규칙을 제거한다.
